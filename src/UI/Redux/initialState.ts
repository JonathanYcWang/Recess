// Initial state is now owned by the background worker via PersistedAppState.
// This file exists only to satisfy legacy imports from unused reducer files.
export const createInitialBlockedSitesState = (): string[] => [];
export const createInitialTimerState = (): unknown => ({});
export const createInitialQuizState = (): unknown => ({});
