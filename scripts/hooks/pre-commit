#!/usr/bin/env sh
# Regenerates .codex/agents/*.toml from .github/agents/*.md before each commit.
# Ensures the generated TOML never drifts from the source personas.
set -e
node scripts/sync-codex-agents.mjs
git add .codex/agents/
